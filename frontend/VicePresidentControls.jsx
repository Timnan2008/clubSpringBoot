import React,{useEffect,useState} from 'react';
import PersonIdentity from './PersonIdentity';
import Avatar from './Avatar';
import {tx} from './language';
export default function VicePresidentControls({club,token,onChanged}){const[team,setTeam]=useState(null),[me,setMe]=useState(null),[selected,setSelected]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const load=()=>Promise.all([fetch('/api/campus-social/me').then(r=>r.json()),fetch('/api/campus-social/clubs/'+club+'/leaders').then(r=>r.json())]).then(([p,t])=>{setMe(p.account);setTeam(t)}).catch(e=>setError(e.message));useEffect(()=>{load();setSelected(null)},[club]);const allowed=me&&(me.role==='admin'||team?.president?.some(p=>p.id===me.id));if(!allowed||!team?.vice_president?.length)return null;return <section className="vice-controls"><h3>{tx('副社长管理','Vice presidents')}</h3><p>{tx('移除本社团副社长职务，保留社员身份和其他社团职务。','Remove the vice-president role for this club. Membership and roles in other clubs remain.')}</p>{team.vice_president.map(p=><div className="vice-control-row" key={p.id}><Avatar person={p}/><PersonIdentity person={p}/><button className="ws-danger-text" disabled={busy} onClick={()=>setSelected(p)}>{tx('移除副社长','Remove vice president')}</button></div>)}{selected&&<div className="vice-confirm"><p>{tx('确认移除副社长：','Remove vice president: ')}{selected.name}？</p><button disabled={busy} onClick={async()=>{setBusy(true);setError('');try{const r=await fetch('/api/campus-social/clubs/'+club+'/officers/'+selected.id,{method:'DELETE',headers:{'X-Workspace-Token':token}});const d=await r.json();if(!r.ok)throw Error(d.message||tx('操作失败','Action failed'));setSelected(null);await load();await onChanged?.()}catch(e){setError(e.message)}finally{setBusy(false)}}}>{tx('确认移除','Confirm removal')}</button><button disabled={busy} onClick={()=>setSelected(null)}>{tx('取消','Cancel')}</button></div>}{error&&<p role="alert">{error}</p>}</section>}
