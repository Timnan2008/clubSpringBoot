// Adapted from ibelick/motion-primitives (MIT). See MOTION-PRIMITIVES-LICENSE.md.
import React,{useEffect,useId,useMemo} from 'react';
import {motion,AnimatePresence,useSpring,useTransform,useReducedMotion} from 'motion/react';
export function AnimatedNumber({value=0}){const reduced=useReducedMotion(),spring=useSpring(value,{stiffness:180,damping:26}),display=useTransform(spring,n=>Math.round(n).toLocaleString());useEffect(()=>{reduced?spring.jump(value):spring.set(value)},[value,reduced,spring]);return <motion.span style={{fontVariantNumeric:'tabular-nums'}}>{display}</motion.span>}
export function TextMorph({children,className=''}){const id=useId(),reduced=useReducedMotion();const chars=useMemo(()=>{const count={};return Array.from(String(children)).map(char=>({id:id+char+(count[char]=(count[char]||0)+1),label:char===' '?'\u00a0':char}))},[children,id]);if(reduced)return <span className={className}>{children}</span>;return <span className={className} aria-label={children} style={{position:'relative',display:'inline-block'}}><AnimatePresence mode="popLayout" initial={false}>{chars.map(c=><motion.span key={c.id} layoutId={c.id} style={{display:'inline-block'}} aria-hidden="true" initial={{opacity:0,filter:'blur(3px)'}} animate={{opacity:1,filter:'blur(0px)'}} exit={{opacity:0,filter:'blur(3px)'}} transition={{type:'spring',stiffness:280,damping:25,mass:.3}}>{c.label}</motion.span>)}</AnimatePresence></span>}
