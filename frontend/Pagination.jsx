import React from 'react';import {tx} from './language';import {pageWindow} from './page-window.mjs';import './Pagination.css';
export default function Pagination({page,pages,onChange}){if(pages<2)return null;return <nav className="number-pagination" aria-label={tx('页码','Pagination')}><button disabled={page<=1} onClick={()=>onChange(page-1)}>{tx('上一页','Previous')}</button>{pageWindow(page,pages).map(n=><button key={n} aria-current={n===page?'page':undefined} onClick={()=>onChange(n)}>{n}</button>)}<button disabled={page>=pages} onClick={()=>onChange(page+1)}>{tx('下一页','Next')}</button></nav>}
