import {useAnimatedClose} from './useAnimatedClose';
import React,{useEffect,useRef,useState} from 'react';
import {FileText,X,ArrowSquareOut} from '@phosphor-icons/react';
import {tx} from './language';
import './PostMedia.css';
function Video({src}){const ref=useRef();useEffect(()=>{const v=ref.current;const observer=new IntersectionObserver(([entry])=>{if(entry.isIntersecting&&entry.intersectionRatio>.65&&!document.hidden){if(v.muted)v.play().catch(()=>{});}else v.pause();},{threshold:[0,.65]});observer.observe(v);const pause=()=>{if(document.hidden)v.pause()};document.addEventListener('visibilitychange',pause);return()=>{observer.disconnect();v.pause();document.removeEventListener('visibilitychange',pause)}},[src]);return <video ref={ref} controls muted playsInline loop preload="metadata" src={src} aria-label={tx('帖子视频','Post video')}/>}
export default function PostMedia({post}){const [opened,setOpened]=useState(null),dialog=useRef();const close=useAnimatedClose(dialog,()=>setOpened(null));useEffect(()=>{if(opened)dialog.current?.showModal()},[opened]);const attachments=post.attachments||[];const url=a=>'/api/campus-social/posts/'+post.id+'/files/'+a.id;const media=attachments.filter(a=>/^(image|video)\//.test(a.type)),files=attachments.filter(a=>!media.includes(a));return <><div className={'post-media-grid media-count-'+media.length}>{media.map(a=>a.type.startsWith('video/')?<Video key={a.id} src={url(a)}/>:<button key={a.id} type="button" onClick={()=>setOpened(a)} aria-label={tx('放大图片','Enlarge image')}><img loading="lazy" decoding="async" src={url(a)} alt={a.name||tx('帖子图片','Post image')}/></button>)}</div>{files.length>0&&<div className="post-files">{files.map(a=><a key={a.id} href={url(a)} target="_blank" rel="noopener noreferrer"><FileText size={24}/><span>{a.name}<small>{Math.ceil(a.size/1024)} KB</small></span><ArrowSquareOut size={18}/></a>)}</div>}{opened&&<dialog className="post-lightbox" ref={dialog} onClick={e=>{if(e.target===e.currentTarget)close()}} onCancel={e=>{e.preventDefault();close()}}><button type="button" className="post-lightbox-close" onClick={close} aria-label={tx('关闭','Close')}><X size={24}/></button><img src={url(opened)} alt={opened.name}/></dialog>}</>}
