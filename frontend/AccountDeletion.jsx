import React,{useState} from 'react';import {tx,tr} from './language';
export default function AccountDeletion({write,account}){const [open,setOpen]=useState(false),[password,setPassword]=useState(''),[confirmation,setConfirmation]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');return <section className="account-card account-deletion"><h2>{tx('注销账号','Delete account')}</h2><p>{tx('永久删除个人资料、装扮、帖子、评论、聊天、日历和所有场地预约，同时退出社团并移除职务。此操作无法恢复。','Permanently delete your profile, appearance, posts, comments, chats, calendar and all court bookings, and remove club memberships and roles. This cannot be undone.')}</p>{!open?<button className="account-danger" onClick={()=>setOpen(true)}>{tx('注销我的账号','Delete my account')}</button>:<form onSubmit={async e=>{e.preventDefault();if(busy)return;setBusy(true);setError('');try{await write('/me/account','DELETE',{password,confirmation});const {forgetIdentity}=await import('./message-crypto.js');await forgetIdentity(account).catch(()=>{});sessionStorage.clear();location.assign('/page/user/login?deleted=1')}catch(e){setError(tr(e.message));setBusy(false)}}}><label>{tx('当前密码','Current password')}<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>{tx('输入 DELETE 确认永久注销','Type DELETE to confirm permanent deletion')}<input value={confirmation} autoComplete="off" onChange={e=>setConfirmation(e.target.value)}/></label>{error&&<p className="account-error" role="alert">{error}</p>}<div className="account-confirm-actions"><button className="account-danger" disabled={busy||confirmation!=='DELETE'||!password}>{busy?tx('正在清理账号数据…','Deleting account data…'):tx('永久注销','Permanently delete')}</button><button className="account-cancel" type="button" disabled={busy} onClick={()=>{setOpen(false);setPassword('');setConfirmation('')}}>{tx('取消','Cancel')}</button></div></form>}</section>}
