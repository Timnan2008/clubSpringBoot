<?php
declare(strict_types=1);namespace MRBS;require 'defaultincludes.inc';require_once 'teacher_queue.inc.php';$account=school_require_account(true);school_queue_install();school_resolve_teachers();header('Content-Type: application/json; charset=utf-8');header('Cache-Control: no-store');$pending=db()->query("SELECT room_id,start_time,end_time,COUNT(*) AS count FROM school_teacher_queue WHERE status='pending' AND start_time>? GROUP BY room_id,start_time,end_time",[time()])->all_rows_keyed();echo json_encode(['pending'=>$pending,'studentOpen'=>school_student_window(),'teacher'=>(int)$account['level']===2]);
