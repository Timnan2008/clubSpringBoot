{
  "name": "qpsw-aero-shards",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild entry.jsx --bundle --minify --format=iife --platform=browser --target=es2020 --jsx=automatic --outfile=../aero-shards.bundle.js"
  },
  "dependencies": {
    "motion": "^12.23.12",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "vgpu": "^0.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
